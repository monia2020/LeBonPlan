{% extends 'base.html.twig' %}

{% block title %}Annonce{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center mb-4">Liste des annonces</h1>

        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Titre</th>
                    <th>Prix</th>
                    <th>Type</th>
                    <th>Description</th>
                    <th>Image</th>
                    <th>actions</th>
                </tr>
            </thead>
            <tbody>
            {% for annonce in annonces %}
                <tr>
                    <td>{{ annonce.id }}</td>
                    <td>{{ annonce.titre }}</td>
                    <td>{{ annonce.prix }}</td>
                    <td>{{ annonce.type }}</td>
                    <td>{{ annonce.description }}</td>
                    <td>
                        <img src="{{ asset('uploads/images/annonces/')}}{{ annonce.image }}" class="images">
                    </td>
                    <td>
                        <a href="{{ path('annonce_show', {'id': annonce.id}) }}">Afficher</a>
                        <a href="{{ path('annonce_edit', {'id': annonce.id}) }}">Modifier</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="7">Pas encore d'annonce</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a href="{{ path('annonce_new') }}">Déposer une annonce</a>
    </div>
{% endblock %}
